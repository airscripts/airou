import logger from './logger.core.js';
import decorator from './decorators.core.js';
import locator from './service.locator.core.js';

export default {
  logger: logger,
  locator: locator,
  decorator: decorator,
};
