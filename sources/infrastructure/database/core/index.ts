import loader from './loader.core.js';
import constants from './constants.core.js';
import decorator from './decorators.core.js';
import locator from './repository.locator.core.js';

export default {
  loader: loader,
  locator: locator,
  decorator: decorator,
  constants: constants,
};
